apiVersion: v1
kind: Service
metadata:
  name: clusterip-service
spec:
  type: ClusterIP
  clusterIP: 10.100.100.101
  selector:
    app: routebackend
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432